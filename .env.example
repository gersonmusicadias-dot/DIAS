# FLUXOMED MARICÁ — variáveis de produção/local
# Nunca publique valores reais deste arquivo.

# PostgreSQL do Supabase. Para Vercel, prefira a URL do pooler/transaction pooler.
DATABASE_URL="postgresql://USUARIO:SENHA@HOST:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# 32+ caracteres. Exemplo de geração:
# node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=""

# URL pública. Em produção: https://seu-projeto.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3100"

# SMTP opcional para convites de usuários.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_REMETENTE="FluxoMed Maricá <nao-responda@seudominio.com.br>"
